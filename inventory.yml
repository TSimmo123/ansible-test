---
all:
  children:
    all-hosts:
      children:
        test-hosts-group:
          hosts:
            test-host:
              ansible-host: 10.0.0.0
              description: "Test Host"
            test-host-2:
              ansible-host: 10.1.0.0
              description: "test2"
